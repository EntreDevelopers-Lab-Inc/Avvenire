import json


# class for handling a bunch of traits
#class TraitHandler


def __init__(self, trait_csv):
    # open the trait csv

    # class to handle merging jpegs


class FileMerger:
    def __init__(self, config_file):
        # load the config
        with open(config_file, 'r') as infile:
            self.data = infile.read()
